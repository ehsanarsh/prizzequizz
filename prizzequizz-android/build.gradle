// Root build file. The Android plugin version is declared here and applied in
// :app, so a single line updates the whole build.
plugins {
    id 'com.android.application' version '8.5.2' apply false
}
